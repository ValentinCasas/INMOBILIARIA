
_Login.cshtml

lo que hago en este archivo es un formulario basado
en la clase LoginView, que lo unico que tiene es usuario
y contraseña.
Lo unico que hace es mandar un LoginView con los datos 
ingresados en los input a el controlador Login que recibe
como parametro un LoginView.


UsuariosController.cs

aca entra a :
public async Task<IActionResult> Login(LoginView login)
mediante post y sin pedir authenticacion, para que los 
usuarios no autenticados puedan acceder.
bueno ahi agregue comentarios a ese metodo para que se entienda que hace
cada parte


Program.cs

ahi hice comentarios de que es cada cosa


Bueno cuestion que yo antes de hashear las contraseñas 
primero intente hacer el login funcional con la contraseña normal,
para corroborar que funciona todo, y recien ahi implemente lo del hash.
y para probar que funciona cree un administrador desde la base de datos, 
con el valor 2 en la columna rol.
